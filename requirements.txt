tweepy==3.9.0
xlrd==1.2.0
openpyxl==3.0.4
contexto==0.1.0
streamlit==1.13.0
altair==4.2.0
plotly==4.10.0
markdown==3.2.2
pytz==2020.1
click==7.1.2
protobuf==3.20.0

# Requeridos directamente por app.py (contexto==0.1.0 ya los instala como
# dependencias transitivas con estas mismas versiones exactas; se listan
# aquí de forma explícita para que quede claro y evitar resoluciones
# inconsistentes de pip).
numpy==1.19.1
pandas==1.1.1
matplotlib==3.3.1
wordcloud==1.8.0

# wordcloud==1.8.0 usa ImageDraw.textsize(), que Pillow eliminó en la 10.x.
# Sin este pin, pip instala el Pillow más nuevo y la nube de palabras truena
# con "AttributeError: 'ImageDraw' object has no attribute 'textsize'".
Pillow==9.5.0